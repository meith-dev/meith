{
  "name": "@meith/worker",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "esbuild --bundle --platform=node --format=cjs --target=node22 --external:@opentelemetry/api --log-override:empty-import-meta=silent src/index.ts --outfile=dist/worker.cjs && esbuild --bundle --platform=node --format=cjs --target=node22 --external:@opentelemetry/api --log-override:empty-import-meta=silent src/migrate.ts --outfile=dist/migrate.cjs"
  },
  "dependencies": {
    "@meith/core": "workspace:*",
    "@meith/drivers": "workspace:*",
    "@meith/runtime": "workspace:*",
    "@meith/tasks": "workspace:*",
    "@meith/db": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tsx": "^4.23.12"
  }
}
