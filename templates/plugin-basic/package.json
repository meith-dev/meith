{
  "name": "meith-plugin-basic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A minimal meith plugin that registers a tool into the shared registry.",
  "meith": {
    "kind": "plugin",
    "entry": "src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
