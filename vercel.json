{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "ignoreCommand": "if [ -n \"$VERCEL_GIT_PREVIOUS_SHA\" ]; then git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- apps/web docs; else git diff --quiet HEAD^ HEAD -- apps/web docs || exit 1; fi",
  "buildCommand": "pnpm --filter @meith/web build",
  "outputDirectory": "apps/web/.next"
}
